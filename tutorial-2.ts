interface User {
    name: string;
    age : number;
    site: object;
}

const user: User ={
    name: "astral",
    age: 20,
    site: {
        blog:"astral",
        github : "tear"
    }
}

const myShow2: {
    first: string;
    second: string;
} = {
    first: "squid",
    second: "name"
}

